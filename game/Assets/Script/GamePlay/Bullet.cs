using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bullet : MonoBehaviour
{
    //[SerializeField] SceneReserch sceneReserch;

    private void Update()
    {
        //if ((sceneReserch.bulletRefLimit != 0) && (sceneReserch.bulletRefCount != 0))
        //{
        //    if (sceneReserch.bulletRefCount == sceneReserch.bulletRefLimit)
        //    {
        //        Destroy(gameObject);

        //    }
        //}
    }

}
